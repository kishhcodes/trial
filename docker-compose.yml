version: '3'

services:
  face-emotion-api:
    build: .
    container_name: face-emotion-api
    ports:
      - "8000:8000"
    volumes:
      - ./known_faces:/app/known_faces
    restart: unless-stopped
    # Uncomment to use your camera (requires additional setup)
    # devices:
    #  - /dev/video0:/dev/video0
